tesla map thing https://tesamuliawati.medium.com/display-json-data-using-react-leaflet-c01ff7670161 
park map thing https://github.com/ugwutotheeshoes/react-leaflet-app-demo/blob/master/src/data/skateboard-parks.json?short_path=ff7e0c0

checkbox
import { useState } from 'react';

export default function MyCheckbox() {
  const [liked, setLiked] = useState(true);

  function handleChange(e) {
    setLiked(e.target.checked);
  }

  return (
    <>
      <label>
        <input
          type="checkbox"
          checked={liked}
          onChange={handleChange}
        />
        I liked this
      </label>
      <p>You {liked ? 'liked' : 'did not like'} this.</p>
    </>
  );
}


 <><Marker
          key={park.map_label}
          position={[park.geo_point_2d.lat, park.geo_point_2d.lon]} /><Popup> {park.map_label} </Popup></>
        />